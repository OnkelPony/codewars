package kata

import "strings"

func ValidBraces(str string) bool {
	bracePairs = map[rune]rune{
		'(': ')',
		'[': ']',
		'{': '}',
	}
	openingBraces = "([{"
	var braces []rune;
	for _, ch := range str {
		if openingBraces.
	}
	return false
}
