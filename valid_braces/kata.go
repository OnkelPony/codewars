package kata

import "strings"

func ValidBraces(str string) bool {
	bracePairs = map[rune]rune{
		'(': ')',
		'[': ']',
		'{': '}',
	}
	lastBrace = str[0]
	for _, ch := range str[1:] {
		if bracePairs.
	}
	return false
}
